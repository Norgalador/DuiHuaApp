$(function(){
    $('.btn-primary').on('click', function(){
        var theme = $(this).data('toggle');
        if (theme === 'light') {
            $('body').addClass('light');
            $('body').removeClass('dark');
        } else {
            $('body').addClass('dark');
            $('body').removeClass('light');
        }
    });
});